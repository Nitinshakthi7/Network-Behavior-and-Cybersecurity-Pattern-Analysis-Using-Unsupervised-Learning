"""
src/anomaly/__init__.py
-----------------------
Anomaly Detection module — NOT YET IMPLEMENTED.

Planned algorithms (after coursework coverage):
    - Isolation Forest     : Efficient, high-dimensional anomaly scoring
    - Local Outlier Factor : Density-based local anomaly detection
    - Statistical scoring  : Z-score / IQR-based pointwise flagging

Integration point in main.py:
    from src.anomaly.isolation_forest import run_isolation_forest
    anomaly_result = run_isolation_forest(X, contamination=config.ANOMALY_CONTAMINATION)
"""
